## MUSIC_LIB mirror backup status

The `MUSIC_LIB` backup from AfricaServer to westmidlands2 is now working correctly as a true rsync mirror.

Working command:

```bash
./sync_musiclib_to_westmidlands2.sh --remote 192.168.4.96 --live --delete
